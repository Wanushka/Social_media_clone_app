import 'package:flutter/material.dart';

class CareersScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Careers")),
      body: Center(child: Text("Careers Page")),
    );
  }
}
